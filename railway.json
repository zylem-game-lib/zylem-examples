{
	"$schema": "https://railway.app/railway.schema.json",
	"build": {
		"builder": "NIXPACKS",
		"nixpacksPlan": {
			"phases": {
				"npm:config": {
					"dependsOn": ["setup"],
					"cmds": [
						"npm config --omit=dev set @tcool86:registry https://registry.npmjs.org",
						"npm config --omit=dev set //npm.pkg.github.com/:_authToken ${NPM_TOKEN}"
					]
				},
				"install": {
					"dependsOn": ["setup", "npm:config"]
				}
			}
		}
	}
}
