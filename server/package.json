{
  "name": "@zylem/spacetime-server",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "SpacetimeDB TypeScript module (entity transform replication) for Zylem multiplayer",
  "scripts": {
    "build": "sh scripts/run-spacetimedb-cli.sh build -p spacetimedb",
    "generate:bindings": "sh scripts/run-spacetimedb-cli.sh generate --lang typescript -o ../src/demos/multiplayer-lobby/spacetimedb-generated -p spacetimedb -y",
    "publish:arena": "sh scripts/run-spacetimedb-cli.sh publish arena -s local -p spacetimedb",
    "publish:multiplayer-lobby": "sh scripts/run-spacetimedb-cli.sh publish multiplayer-lobby -s local -p spacetimedb",
    "publish:local": "pnpm run publish:arena && pnpm run publish:multiplayer-lobby",
    "start:server": "bash scripts/start-spacetimedb-server.sh",
    "dev": "pnpm run build && pnpm run publish:local"
  }
}
